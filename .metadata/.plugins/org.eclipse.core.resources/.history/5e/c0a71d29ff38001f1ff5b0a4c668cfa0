<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8">
<title>templates/sub/notice.html</title>
</head>
<body>
	<div class="container">
		<h3>공지사항</h3>
		<!-- /*
			th:each 를 이용해서 반복적으로 마크업을 출력할 수 있다.
			list 는 List<String> type 이기 떄문에 tmp 는 String type 이다
			String type 이기 떄문에 직접 출력 가능하다
		*/ -->
		<ul>
			<li th:each="tmp:${list}">[[${tmp}]]</li>
			<li></li>
		</ul>
		
		<h3>공지사항2</h3>
		<ul>
			<li th:each="tmp:${list}" th:text="${tmp}" ></li>
		</ul>
		
		<h3>공지사항2</h3>
		<ul>
			<li th:each="tmp, status:${list}">
				count : [[${status.count}]]
				index : [[${status.index}]]
				size  : [[${status.size}]]
				even  : [[${status.even}]] <!-- /*인덱스 짝수 여부*/ -->
				odd   : [[${status.odd}]] <!-- /*인덱스 홀수 여부*/ -->
				first : [[${status.first}]]
				last  : [[${status.last}]]
				current : [[${status.current}]]
			</li>
		</ul>
	</div>
</body>
</html>